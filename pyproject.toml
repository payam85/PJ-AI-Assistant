[project]
name = "pj-career-assistant"
version = "0.1.0"
description = "Local career assistant with reviewable drafts and cached specialist workflows"
license = {file = "LICENSE"}
requires-python = ">=3.11"
dependencies = ["openai-agents==0.18.3", "openai==2.48.0", "pydantic==2.13.4", "python-dotenv==1.2.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
